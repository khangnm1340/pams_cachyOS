
###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = ALT # Sets "Windows" key as main modifier

bind = $mainMod, F1, exec, ~/.config/hypr/scripts/gamemode.sh
bind = $mainMod, Q, exec, $terminal
bind = ALT_SHIFT, B, exec, ~/.config/hypr/scripts/waybar.sh
bind = SUPER, Space, exec, ~/.config/hypr/scripts/nordvpn.sh
bind = ALT_SHIFT, Q, exec, cd ~/dotfiles && neovide
# bind = ALT_SHIFT, Q, exec, export NVIM_APPNAME=nvim-hanni && cd ~/dotfiles/pamsdots && neovide
# bind = ALT_SHIFT, Q, exec, neovide --title-hidden
# bind = ALT_SHIFT, I, exec, qutebrowser
# bind = $mainMod, I, exec, alacritty -T fzf -e bash -ic "fd -E 'in*' -E 'com.zamaudio*' . /usr/share/applications -x basename {} | fzf --reverse | xargs gtk-launch"
# bind = $mainMod, I, exec, ~/.config/hypr/scripts/fzf-launcher.sh

# bind = $mainMod, I, exec, alacritty -T fzf -e bash -ic 'selection=$(fd -E "in*" -E "com.zamaudio*" . /usr/share/applications -x basename {} | fzf --height=40% --reverse --prompt="App> "); if [ -n "$selection" ]; then gtk-launch "$selection"; else echo "Cancelled"; fi'

bind = ALT_SHIFT, D, exec, clispe -clear
bind = ALT_SHIFT, N, exec, hyprpicker -la
# bind = $mainMod, b, exec, ghostty -e rmpc
bind = $mainMod , period, exec, swaync-client --hide-latest
bind = $mainMod, slash , exec, $HOME/.config/hypr/scripts/theme_switch.sh
# bind = $mainMod, I, exec, rofi -show run
# bind = $mainMod, I, exec, walker
bind = $mainMod, I, exec, ~/dotfiles/dot-config/hypr/scripts/anyrun.sh
# bind = $mainMod, I, exec, ~/.config/hypr/scripts/fzf-launcher.sh
bind = $mainMod, W, killactive,
# bind = $mainMod, M, exec, hyprlock
bind = $mainMod SHIFT, M, exec, systemctl suspend
bind = $mainMod, E, exec, $fileManager --new-window
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
# bind = $mainMod SHIFT, C, pseudo, # dwindle
# bind = $mainMod, C, togglesplit, # dwindle
bind = $mainMod, F, fullscreen,1
bind = $mainMod SHIFT, F, fullscreen,3
# bind = ALT , Tab , layoutmsg, cyclenext ,
# bind = ALT SHIFT , Tab , cycleprev

bind = ALT, Tab, cyclenext         # Change focus to another window
bind = ALT, Tab, bringactivetotop  # Bring it to the top
# bind = ALT SHIFT, Tab, cyclenext, prev
# bind = ALT SHIFT, Tab, bringactivetotop

bind = $mainMod SHIFT, Tab, exec, ~/.config/hypr/scripts/gamemode.sh

bindd = SUPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle

# bind = SUPER SHIFT, I, exec, $HOME/dotfiles/dot-config/hypr/scripts/toggle-idle.sh


bind = SUPER, U, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.2')
bind = SUPER_SHIFT, U, exec, hyprctl keyword cursor:zoom_factor 1.0



bind = SUPER, M, exec, nu ~/.config/hypr/scripts/randomly-load-wallpaper.nu
# bind = $mainMod SHIFT, I, exec, mpc toggle
#screenshot
bind = ALT_SHIFT, S, exec, hyprshot -zm region -o $HOME/Pictures/Screenshots/is-hyprland 
bind = SUPER_SHIFT, S, exec, hyprshot -zm output -o $HOME/Pictures/Screenshots/is-hyprland 
bind = ALT_CTRL , U, exec, easyeffects -l "normal stuff"
bind = ALT_CTRL , I, exec, easyeffects -l "LoudnessEqualizer"
bind = , XF86Calculator, exec, qalculate-qt

# bind = $mainMod, M, exec, systemctl sleep
# bind = SUPER, R, exec, ~/.config/hypr/scripts/wl_screenrec.sh
bind = $mainMod, BackSpace, exec, ~/dotfiles/dot-config/hypr/scripts/repl-workaround.sh

#
# bind = some, key, sendshortcut, ,Space,



bind = ALT_CTRL, K, movetoworkspace, +1
bind = ALT_CTRL, J, movetoworkspace, -1
bind = SUPER, k, workspace, +1
bind = SUPER, j, workspace, -1
# binde = $mainMod, L, resizeactive, 40 0
# binde = $mainMod, H, resizeactive, -40 0

# bind = ALT_CTRL, H, exec, notify-send "this key works"
bind = SUPER_SHIFT, H, movewindow, l
bind = SUPER_SHIFT, L, movewindow, r
bind = SUPER_SHIFT, k, movewindow, u
bind = SUPER_SHIFT, j, movewindow, d



# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8

bind = ALT_CTRL, 1, movetoworkspace, 1
bind = ALT_CTRL, 2, movetoworkspace, 2
bind = ALT_CTRL, 3, movetoworkspace, 3
bind = ALT_CTRL, 4, movetoworkspace, 4
bind = ALT_CTRL, 5, movetoworkspace, 5



# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod CTRL, S, movetoworkspace, special:magic
bind = $mainMod, D, togglespecialworkspace, scratchpad
bind = $mainMod CTRL, D, movetoworkspace, special:scratchpad


# Scroll through existing workspaces with mainMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1
#

bind = $mainMod, B, submap, resize # Press SUPER + R to enter resize mode
submap = resize
  binde = , h, resizeactive, -200 00 # Shrink down-left
  binde = , l, resizeactive, 200 00  # Grow right, shrink up
  binde = , j, resizeactive, 00 200  # Grow right, shrink up
  binde = , k, resizeactive, 00 -200  # Grow right, shrink up
  bind = , escape, submap, reset # Press Escape to exit resize mode
submap = reset

# binde = SHIFT_SUPER, h, resizeactive, 60 0
# binde = SHIFT_SUPER, l, resizeactive, -60 0
# binde = SHIFT_SUPER, j, resizeactive, 0 60
# binde = SHIFT_SUPER, k, resizeactive, 0 -60

# Scroll Up
# binde = SUPER_SHIFT, K, exec, wlrctl pointer scroll 120 0 # Mouse button 4 is scroll up
binde = SUPER_SHIFT, J, exec, echo wheel -3 | dotoolc
binde = SUPER_SHIFT, K, exec, echo wheel 3 | dotoolc
# Scroll Down

# windowrule = opacity 0.90 0.90,class:zen
bind = $mainMod, P, exec, ~/.config/hypr/scripts/clipse.sh
bind = $mainMod, Y, exec, ~/.config/hypr/scripts/hyprsunset.sh
bind = ALT_SHIFT, D, exec, clipse --clear


# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod , mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod, N, exec ,swaync-client -t -sw
bind = $mainMod CTRL, N, exec ,swaync-client -C




# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# bind= Ctrl,escape,exec,wlrctl pointer click left
# bind = ALT,escape,exec, wlrctl pointer click left
# bind=ALT_SHIFT,a,exec,hyprctl dispatch submap reset && wl-kbptr
# Move focus with mainMod + arrow keys
# binde=SUPER,j,exec,wlrctl pointer move 0 20
# binde=SUPER,k,exec,wlrctl pointer move 0 -20
# binde=SUPER,l,exec,wlrctl pointer move 20 0
# binde=SUPER,h,exec,wlrctl pointer move -20 0

#hyprscrolling
# move between columns
# bind = SUPER, h, layoutmsg, move -col
# bind = SUPER, l, layoutmsg, move +col
# bind = SUPER SHIFT, h,  layoutmsg, swapcol l
# bind = SUPER SHIFT, l, layoutmsg, swapcol r
# bind = SUPER, o,  layoutmsg, togglefit
# resize focused column
# bind = $mainMod, minus, layoutmsg, colresize -conf
# bind = $mainMod, equal, layoutmsg, colresize +conf
bind = $mainMod, M, exec, /home/pampam/.config/hypr/scripts/toggle_screen.sh




# bind = SUPER CTRL, j, movewindow, mon:+1
# bind = SUPER SHIFT, j, focusmonitor, +1
# bind = SUPER, D, exec, hyprctl dispatch movetoworkspacesilent special:scratchpad
# bind = SUPER SHIFT, D, exec, hyprctl dispatch focusworkspace back_and_forth
# bind = $mainMod , D, togglespecialworkspace, scratchpad

#NOTE: Old style
#
# animations {
#     enabled = yes, please :)
#
#     # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#
#     bezier = easeOutQuint,0.23,1,0.32,1
#     bezier = easeInOutCubic,0.65,0.05,0.36,1
#     bezier = linear,0,0,1,1
#     bezier = almostLinear,0.5,0.5,0.75,1.0
#     bezier = quick,0.15,0,0.1,1
#
#     animation = global, 1, 10, default
#     animation = border, 1, 5.39, easeOutQuint
#     animation = windows, 1, 4.79, easeOutQuint
#     animation = windowsIn, 1, 3.1, easeOutQuint, popin 87%
#     animation = windowsOut, 1, 1.49, linear, popin 87%
#     animation = fadeIn, 1, 1.73, almostLinear
#     animation = fadeOut, 1, 1.46, almostLinear
#     animation = fade, 1, 3.03, quick
#     animation = layers, 1, 3.81, easeOutQuint
#     animation = layersIn, 1, 4, easeOutQuint, fade
#     animation = layersOut, 1, 1.5, linear, fade
#     animation = fadeLayersIn, 1, 1.79, almostLinear
#     animation = fadeLayersOut, 1, 1.39, almostLinear
#     animation = workspaces, 1, 1.94, almostLinear, fade
#     animation = workspacesIn, 1, 1.21, almostLinear, fade
#     animation = workspacesOut, 1, 1.94, almostLinear, fade
# }

